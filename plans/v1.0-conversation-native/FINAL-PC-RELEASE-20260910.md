# PC 最终候选版收尾 · 2026-09-10

## 范围与门槛

用户确认 PC 优先，并要求继续迭代到完整版本输出。沿用 1.0 源码 RC 能力合同：两个内置训练路径（图片分类、表格回归）、一个批准注册后的音频关键词路径，以及公开模型静态研究/诊断。任意第三方模型执行仍须独立隔离、QualificationRun 和人工注册证据；不绕过 AGENTS.md 的安全门。

PC 视口为 1280×800、1440×900、1920×1080。根据用户明确要求，手机验收不再是本次发布必需项；其余训练验收门、最终测试集政策、证据身份及发布门均不变。

## 执行顺序

1. 在独立测试工作区通过公开启动器启动真实 Agent 和训练引擎，验证完整桥接授权通道。
2. 用仓库公开的开发验收 CSV 从浏览器走完需求、数据体检、合同确认、训练、评测、新样本和 Bundle 下载；只操作该测试任务。
3. 在对应流程修复 PC 信息层级、动作入口和真实状态问题，验证无效数据、停止/重试、刷新、列表重入及重启。
4. 复验已注册三条训练路径；对未支持方向保存精确 BlockerEvidence，验证绝不产生模拟 Run。
5. 全量测试、发布资料和可复验源代码包；冻结精确提交后独立检查远端、CI、冷克隆与发布状态。不把本地验收当成线上发布。

## 基线

- 源分支：`codex/v1.0-conversation-native`，HEAD `bee554c1071969d04d1faffec817bbb9545d08ea`。
- 继承上一轮未提交修复：625 项 Python / 167 项 Node 通过，PC 连续对话五轮验证；尚无本轮训练交付全链路证据。
- 8877 旧预览继续保留。新测试使用独立端口和 runs，不操作用户现有任务。
- GitHub 上同名分支目前也是该 HEAD；无 v1.0 RC tag。发布状态未变。
- 本机未发现 Docker/Podman 可执行文件；任意第三方仓库执行保持 analysis-only，不能声称隔离执行已具备。

## 发现与验收账本

随实际执行记录，不继承旧版本“已通过”结论。每项必须包含 task/run/object ID、持久化、同一身份、重入、状态约束和错误/取消证据。

### 本轮修复

- 对话检查点可先讨论再决策；未批准的普通追问不被转换为批准或拒绝，取消旧检查点后继续同一任务。
- 审批标题由精确工具身份决定，来源绑定明确展示完整 commit 和只读范围，不再误标为数据导入。
- 续跑子会话读取完整分页历史，以父调用回执及子接收记录绑定调用区间；模糊血缘失败关闭。projector 升至 3.3，历史只读检查点可安全重投影。
- 显式停止以原生取消事件为依据，修复启动瞬间 idle 竞态；恢复后的同一任务不会假完成或卡在运行中。
- 页头、当前回合与任务列表使用同一任务身份和真值投影。历史工具错误保留审计，但不污染后续独立回合。
- 冷克隆发现静态仓库分析复用了“后台训练”提示；所有通用后台提示改为“后台操作”，具体操作仍由真实工具记录说明，避免将来源分析误称为训练。
- 冷克隆再次发现明确排除的“不是时间序列 / 不使用历史序列”被时序冲突校验当成肯定需求，导致重复确认。修复仅过滤局部明确否定的时序术语，不改写已保存目标；肯定时序需求、双重否定及结构化时序 modality 仍保留冲突保护。新增中英文排除、肯定反例和连续 TaskSpec 修订回归。
- 右侧交付包、评测、推理输入和阻断证据先给可读摘要，精确身份与原始 JSON 可展开；不猜测缺失 ObjectRef。
- 数据上传失败保留已有确认与当前结果；启动预检有超时和子进程清理，锁定 runtime 依赖修复已知 qs 漏洞。

### A. 房屋售价估算：浏览器真实训练与交付

| 对象 | 精确身份 / 结果 |
| --- | --- |
| Task / spec | `task-d1b89f0175780fec2fa46661893a8407` / 4 |
| 根会话 | `session-fda85fab-bbf3-4c26-97be-1a05f0bd7796` |
| Dataset | `dataset-facb20674c`；120 行公开开发验收数据 |
| 数据摘要 | `c52210321392e804eee171b33601c36a5787a56256ad28b4649c3411032e49aa` |
| 已确认合同 | `contract-revision-b04d33a6fd83` |
| 合同摘要 | `b901ac6b1433e343191dbcdda6c9ccd4b58e3fcb5f4c83e68f08c095c9f86745` |
| Run | `20260910T105451244424Z-task-d1b89f0175780fec2fa46661893a8407` |
| Evaluation | `evaluation-a962235e9cff` |
| 评测摘要 | `81ba0e1408f4f117ad055fe5849e5f39a92951676741bf961e7c1da289c10127` |
| 评测结果 | 完整性 14/14，指标门 5/5；24 条 held-out test，未污染；允许发布审阅 |
| 新输入 | `inference-input-67e1f7c66f72`；面积 88.5、卧室 2、楼龄 7.3 |
| 推理检查 | `sample-8336968c3ff2`；预测 `367072.38129424746`，无标准答案，不作为准确率证据 |
| Bundle | `bundle-b1ecc1c88f90`；9 个文件及清单，9692 字节 |
| Manifest 摘要 | `ccd63f7f0c7d7070cb51f3645270d47df978312a316a014eafc4f812b7839078` |
| 已下载 ZIP 摘要 | `a57402e2efd2faadc1fbfeac93fa4a8228ace6aee0a8ae69b91925f760a07c21` |

通过浏览器原生数据导入、合同确认、训练批准、新样本问题、推理批准、Bundle 生成和下载批准完成。刷新、列表重入、后端重启后身份不变；续用同一个评测子会话的三个调用分别绑定评测、推理与交付所属回合。14 条旧检查点请求/解决记录精确重投影为只读历史，不再出现待批准。

交付清单确认不包含原始数据、最终测试引用或内部运行状态。无效 1 行 CSV 经真实上传返回 422；原 Dataset、spec 4、已确认合同和当前 Run 均保持不变，前端给出失败原因和重试入口。

### B. ASR：固定来源、讨论授权、静态诊断与停止恢复

| 对象 | 精确身份 / 结果 |
| --- | --- |
| Task | `task-ea13716681992f2e41df33c9fb26ae1f` |
| 根会话 | `session-61d1be02-3d70-409c-b91a-cbdf0e36c199` |
| 来源 | Hugging Face `openai/whisper-tiny` |
| 固定 commit | `169d4a4341b33bc18d8881c4b69c2e104e1cc0af` |
| 来源解析 | `source-resolution-r1-32bc8478304e` |
| 能力阻断 | `blocker_9344d7deccdc23d4dfea57de` / `recipe_unavailable` |
| 能力阻断摘要 | `9344d7deccdc23d4dfea57de670d509f9f6f190b39d97e51b6a2d3103c892ecd` |
| 人工批准后的绑定 | `model-binding-r1-895435705afb` |
| 静态分析 | `analysis_bf7821159763b9e624ff` |
| 分析摘要 | `7e0cd8ffbe4eb2225be8c2323f175a26c3c84a38b73674413ec5c7614a5fb508` |
| 安全阻断 | `blocker_5fbc9c662fc00a4a81e7d666` / `blocked_security` |
| 安全阻断摘要 | `5fbc9c662fc00a4a81e7d6661f04a3b9260846bb5e5a0ecb837373aa4e0a1ba2` |
| 当前 Run | 无；未下载权重、未安装依赖、未执行第三方代码 |

绑定待批准期间，用户先询问是否等于下载执行，旧批准被暂停且绑定数仍为 0；AI 回答后再次明确提出同一 commit 的绑定申请，经原生按钮批准才进入静态分析。权重仓库的 `.bin` 风险触发保守安全策略；这不是 ASR 模型试跑，也不证明资源执行可行。

真实停止记录：AgentRun `agent-run-b91b2946763e4ca382d82cff9339a023`，AgentTurn `agent-turn-dd80a238a6f24a21ad3a511333d06c38`，取消审计 `cancel-request-26fee5e43e1b441592dd2f68a9de7979`。填写理由并提交取消后原生 `turn_cancelled(reason=aborted)` 留档。修复后重启重放为 `cancelled`；随后在同一 Task / session 的 `agent-run-6b06169e79784135b06cacd29833310e` 正常回答，旧阻断身份不变。仅打开取消弹窗不算取消证据。

### PC、自动化与保留状态

- `scripts/verify_project.py`：Python **646**、Node **181** 全部通过，适配器语法检查通过。包括图片、表格、声明式音频路径及否定时序意图的自动化回归；图片和音频不冒称本轮重新进行浏览器训练。
- 冷克隆发现阻断证据查看器误把 ObjectRef 的 `content_digest` 当作 `semantic_digest` 比对。修复为精确内容摘要匹配；新增异任务、异 ID、异摘要、缺摘要及语义摘要替代均拒绝的回归，未降低身份门。
- 冷启动产生了真实专家子任务的 `turn_cancelled`，主协调器仍在批准点等待。终止记录现在按观察到的 root/session/delegation 身份区分：专家终止折叠保留，不冒充整轮停止，也不显示整轮恢复按钮；归属不全时明确待核对。
- 真实外网超时的取消/重试发现：已经终止、worker 已退出的历史 `cancel_requested` 审计标记不能继续锁死新批准卡；仍存活的 worker 或尚在 `cancel_requested` 状态的操作继续阻止批准。前端两处状态归约已统一此边界，并增加恢复与未停稳负例。
- 2026-09-11 的重启验收发现：DSH 中断投影的终止时间可能早于审批通知接收时间约 80ms，且重放不再包含原始审批事件，旧卡因此误留为待批准。修复从同一 RPC 的完整持久审计恢复观察过的 run/turn/call 身份，按精确所属回合失效旧卡；不同回合、不同 RPC/批准/调用身份和歧义血缘不得复用。没有自动批准、重新执行或删除历史；普通后续消息仍通过原生 session.prompt 恢复同一会话。
- 来源研究子会话的审批策略为 never，不能承担人工绑定批准。来源选择、BYOM 绑定和 HF 资产绑定现在在子会话权限检查时明确退回根协调器；不再让用户反复回答普通批准问题后收到系统拒绝。根协调器仍需原生批准，不扩大子会话权限，也不自动执行。
- 三种 PC 尺寸各检查两个真实任务：无横向溢出、无裁切/屏外元素，独立页面审计控制台错误与 page error 均为 0。普通段落链接的行高及品牌装饰图空 alt 属于明确的非操作性审计例外；证据按钮最小高度已调整为 36px。
- 交付包、完整 commit 批准范围、失败附件、历史检查点和当前阻断状态已人工看图复核。主测试会话含主动制造的离线与 422 错误，不以该会话宣称控制台始终为 0。
- 用户原 8877 预览的“月租金估算”任务 `task-67ec6af52225d17fe89f79cb45c2f705`、spec 3、原 session 及五轮历史保留；只更新代码运行实例。8878 为独立验收工作区。本地预览不是公网或生产部署。
- 原始运行状态、生成制品、截图与测试日志位于 Git 忽略目录或临时验收空间，不作为源码提交。仓库仅保留小型公开输入及验证逻辑。

## 冻结后独立发布门

本文记录冻结前实测，不用历史通过替代精确提交验收。冻结后的远端 SHA、锁文件安装、CI、冷克隆双旅程及重启证据须在对应发布附件中独立记录；在这些门通过前，不创建表示通过的 RC Tag / Prerelease。源码中的 `unreleased_rc` 是静态构建标记，不证明远端发布状态；发布事实必须查验 GitHub 的 Tag、Prerelease、精确提交与附件摘要。

最终交付范围是 **PC 本地优先源码 RC**，不是“任意模型都可训练”的通用生产版。无多用户鉴权，不直接暴露公网；未验证隔离后端的外部仓库始终 analysis-only。
