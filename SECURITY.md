# Security

## 不应提交的内容

- API Key、云平台凭证、SSH Key和Cookie；
- 客户合同、工业现场数据、个人语音和其他受限数据；
- 未确认再分发权利的数据集或模型权重；
- 包含个人路径、内部域名或身份信息的运行日志。

## 执行边界

模型训练会处理外部数据、下载第三方依赖，并可能执行Agent生成的代码。生产或敏感任务应使用隔离容器、最小文件权限、受控网络和明确计算预算。

Joblib与Pickle模型在加载时可能执行代码。只对可信来源且哈希匹配的本地产物使用深度验证。

## 报告问题

在公共仓库建立前，本项目只接受本地审查。发现泄露风险时，应先停止发布并移除敏感制品，再重新生成干净的发布候选。
